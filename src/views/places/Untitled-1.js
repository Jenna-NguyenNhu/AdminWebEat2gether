onClick={(e) => deletePlace(row.id)}{/* <div>
    {userRData.map((item, key) => (
      <div key={key}>
        <td>
          <strong>Content: </strong>
          {item.content}
        </td>
      </div>
    ))}
  </div> */}